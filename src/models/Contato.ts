class Contato {
  nome: string
  email: string
  numeroTel: string
  id: number

  constructor(nome: string, email: string, numeroTel: string, id: number) {
    this.nome = nome
    this.email = email
    this.numeroTel = numeroTel
    this.id = id
  }
}

export default Contato
